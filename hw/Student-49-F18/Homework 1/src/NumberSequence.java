import java.util.List;

/**
 * Number sequence interface that has all the methods for outputting a series
 * of numbers.
 */
public interface NumberSequence {

  /**
   * Method for a Number Sequence that outputs
   * @return a list containing sequence from integer a to integer b
   */
  List outputNum(int a, int b);
}
